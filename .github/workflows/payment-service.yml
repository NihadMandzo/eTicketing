name: Deploy payment-service

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/services/payment/**'
      - 'backend/shared/**'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

# Best practice, not optional: without this, two quick pushes race to update the
# same Container App and whichever `az containerapp update` lands second wins —
# which might be the older commit. One in-flight deploy per app, always.
concurrency:
  group: deploy-payment-service
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: azure/login@v3
        with:
          client-id: ${{ secrets.IDENTITYSERVICE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.IDENTITYSERVICE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.IDENTITYSERVICE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push image
        run: az acr build -r eticketingacr -t payment:${{ github.sha }} -f backend/services/payment/eTicketing.Payment.Api/Dockerfile backend

      - name: Deploy to Container App
        run: az containerapp update -n payment-service -g eticketing-dev --image eticketingacr.azurecr.io/payment:${{ github.sha }}
